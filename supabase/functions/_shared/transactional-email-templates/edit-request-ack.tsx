import * as React from 'npm:react@18.3.1'
import { Body, Button, Container, Head, Heading, Hr, Html, Preview, Section, Text } from 'npm:@react-email/components@0.0.22'
import type { TemplateEntry } from './registry.ts'
import { main, container, accentStrip, brand, h1, lead, body, button, hr, footer } from './_brand.ts'
interface Props { senderName?: string; shortCode?: string; occasion?: string; trackUrl?: string }
const EditRequestAckEmail = ({ senderName, shortCode, occasion, trackUrl }: Props) => <Html lang="en" dir="ltr"><Head /><Preview>We received your edit request — {shortCode}</Preview><Body style={main}><Container style={container}><div style={accentStrip} /><Heading style={brand}>Xenium</Heading><Heading style={h1}>We've got your edit request</Heading><Text style={lead}>Thanks, {senderName ?? 'there'} — your notes for your {occasion ? occasion.toLowerCase() : 'Xenium'} are safely with us.</Text><Text style={body}>A human from our design team will review and action your request within 24 hours. We'll keep your experience moving and let you know when the updated version is ready.</Text><Section style={{ textAlign: 'center', margin: '24px 0 8px' }}><Button href={trackUrl} style={button('primary')}>Track your order</Button></Section><Hr style={hr} /><Text style={footer}>Order {shortCode}</Text><Text style={footer}>Xenium · Hand-crafted personalised digital gifts · Made in India</Text></Container></Body></Html>
export const template = { component: EditRequestAckEmail, subject: (d: Record<string, any>) => `We received your edit request — ${d.shortCode ?? ''}`, displayName: 'Edit request acknowledgement', previewData: { senderName: 'Rohan', shortCode: 'XEN-7K9P2A', occasion: 'Anniversary', trackUrl: 'https://xenium-sites.com/track/XEN-7K9P2A' } } satisfies TemplateEntry
